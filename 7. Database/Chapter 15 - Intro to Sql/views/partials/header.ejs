<header class="bg-red-500 text-white p-4 shadow-md">
  <nav class="container mx-auto flex items-center gap-6">

    <a href="/" class="<%= currPage==='index' ? 'bg-red-400': 'hover:bg-red-400' %>" >
      AirBnB
    </a>

    <a href="/homes" class="<%= currPage==='Home' ? 'bg-red-400': 'hover:bg-red-400' %>" >
      Homes-List
    </a>

    <a href="/bookings" class="<%= currPage==='bookings' ? 'bg-red-400': 'hover:bg-red-400' %>" >
      Bookings
    </a>

    <a href="/favourites" class="<%= currPage==='favourites' ? 'bg-red-400': 'hover:bg-red-400' %>" >
      Favourites
    </a>

    <a href="/host/host-homes" class="<%= currPage==='host-homes' ? 'bg-red-400': 'hover:bg-red-400' %>" >
      Host Homes
    </a>
    <a href="/host/add-home" class="<%= currPage==='addHome' ? 'bg-red-400': 'hover:bg-red-400' %>" >
      Add Home
    </a>

  </nav>
</header>